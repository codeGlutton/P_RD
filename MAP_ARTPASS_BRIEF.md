# 코덱스에게 줄 지시문 — 지도 아트 통일 패스 (이미지만!)

> 코덱스에는 아래를 그대로 복붙한다. **코덱스는 PNG만 만든다.**
> 임포트·WBP·코드는 코덱스가 하지 않는다 (이쪽에서 처리).
> 지금까지 만든 것 중 **살아있는 것**: 룸 아이콘 6종, 팝업 배경(정사각 2048).
> **폐기**: 나무 책상 배경. 이번에 **교체**: 지도 본체, 연결선, 프레임/버튼.

---

## 복붙용 지시문

이미지 생성 작업이다. **코드·에셋·WBP·Config는 절대 건드리지 마라.**
ImageGen으로 아래 PNG 5장을 만들어 `시안4_7\지도_final\` 폴더에 저장하고 끝내라.
임포트도 하지 마라. 적용은 다른 쪽에서 한다.

### 배경 지식 — 화면이 어떻게 생겼는가
세로로 긴 지도가 화면에 팝업으로 뜨고, 그 위에 **원형 룸 아이콘**들이 격자로
얹히며 아이콘끼리 **선**으로 이어진다. 우측 하단에 **범례 패널**, 우측 상단에
**BACK 버튼**이 떠 있다. 지도 뒤에는 짙은 네이비 배경(이미 완성)이 깔린다.

**이번 작업의 목적은 딱 하나: 아이콘이 지도에 묻히는 문제를 해결하는 것이다.**
지금 지도가 너무 화려하고 디테일이 많아 아이콘이 안 보인다. 지도는 무대이고
아이콘이 주인공이다. 아래 5장은 전부 그 원칙으로 그린다.

### 공통 톤 (5장 전부)
- 이미 만든 룸 아이콘(`room_icons_v2.png`)의 **짙은 네이비 원판 + 은색 링 +
  금색 리벳** 조합이 이 화면의 기준색이다. 프레임·선은 이 금속 계열을 따른다.
- 배경(`map_popup_background_2048.png`)은 짙은 네이비다. 프레임류는 이 위에
  얹혀도 읽혀야 한다.

---

### 이미지 1 — 지도 본체 (★가장 중요, 완전히 다시 그린다)
`map_scroll_flat_v3.png` / **1024×3072 (세로:가로 = 3:1)**

기존 `map_scroll_flat_v2.png`와 **구성·지형 흐름은 같게, 화려함만 걷어낸다.**
- 지형 흐름(아래→위, 끊김 없는 연속 변화):
  1. 하단 1/3: 숲속 마을 — 통나무집·오솔길
  2. 중단 1/3: 어두워지는 숲 — 버려진 전초기지, 마른 나무, 안개
  3. 상단 1/3: 묘지·망령불 → **맨 위에 언데드 성문**
- 길 하나가 아래 끝에서 위 성문까지 끊기지 않고 이어진다.

**★여기가 핵심 — 눌러서 그려라:**
- **채도를 크게 낮춰라.** 거의 단색화에 가깝게. 초록 잔디·붉은 지붕 같은
  원색 금지. 전체를 **바랜 세피아~옅은 올리브 단일 톤**으로 통일하고, 상단
  묘지 구역만 아주 살짝 차갑게 기울인다.
- **명암 대비를 낮춰라.** 새까만 그림자, 새하얀 하이라이트 금지. 중간톤 위주.
- **디테일을 줄여라.** 집·나무·바위는 개별 묘사 대신 **실루엣과 큰 덩어리**로.
  창문 하나하나, 지붕 기와 한 장 한 장, 풀잎 무늬 같은 잔무늬는 전부 빼라.
- 망령불·등불 같은 **발광 요소를 넣지 마라.** 아이콘보다 밝은 점이 화면에
  있으면 안 된다.
- 판단 기준: 이 그림을 눈 반쯤 감고 봤을 때 **거의 균일한 갈색 판**으로
  보여야 성공이다. 개별 오브젝트가 도드라지면 실패다.

**형식 제약:**
- **완전히 평평한 직사각형.** 원근·사다리꼴·기울임 절대 금지 (게임 엔진이
  실시간으로 원근을 건다. 이미지에 있으면 이중 왜곡된다).
- 양피지 테두리 장식을 네 변에 포함하고, 그림이 캔버스를 가득 채운다(풀블리드).
- 위·아래 끝 각 5%는 화면 밖으로 나가니 중요한 그림을 두지 마라.
- 알파·크로마키 불필요. 불투명.

---

### 이미지 2 — 연결선: 열린 길 (실선)
`map_path_solid.png` / **256×32 (가로로 긴 띠)**

- 아이콘과 아이콘을 잇는 선이다. 게임에서 이 띠를 **가로로 반복 타일링**해
  길이를 만들고, 임의 각도로 회전시킨다.
- 따라서: **좌우 끝이 이어붙어도 이음새가 안 보여야 하고**(가로 심리스),
  **상하 대칭**이어야 한다(회전해도 위아래가 같아야 함).
- **★납작하게 눌려 그려진다.** 실제 화면에서는 가로 256 : 세로 14 비율로
  압축된다(약 2.3배 세로 압축). 그러니 **세로 방향 디테일을 넣지 마라** —
  꼬임 무늬·다중 하이라이트 같은 건 뭉개져서 지저분해질 뿐이다. 단순한
  띠 하나에 위아래 외곽선 정도가 적당하다.
- 모양: 아이콘 링과 같은 계열의 **금색/황동 밧줄 또는 금속 띠**. 가장자리에
  어두운 외곽선을 넣어 어떤 배경 위에서도 읽히게 한다.
- 지도가 눌린 톤이므로 이 선은 **또렷하고 밝게** — 경로가 한눈에 읽혀야 한다.
- 배경은 투명(알파) 또는 크로마키.

### 이미지 3 — 연결선: 잠긴 길 (점선)
`map_path_locked.png` / **256×32**

- 이미지 2와 **같은 두께·같은 타일 규격·같은 세로 압축 조건**. 대신 끊어진
  점선이고, 색은 채도 빠진 **회색/납빛**에 밝기도 낮다 — "아직 못 가는 길"로
  읽혀야 한다.
- 점 간격이 256px 안에서 정확히 반복되게 배치해 이어붙였을 때 리듬이 깨지지
  않게 하라. 점은 **가로로 길쭉한 대시**가 좋다(세로로 눌리므로 동그란 점은
  찌그러진 타원이 된다).
- 배경은 투명(알파) 또는 크로마키.

---

### 이미지 4 — 패널 프레임 (범례용)
`ui_panel_frame.png` / **512×512**

- 우측에 뜨는 범례 상자의 틀이다. 안에는 아이콘 6개와 글자가 세로로 들어간다.
- 모양: 짙은 네이비 판 + **은색 금속 테두리 + 모서리 금색 리벳** (룸 아이콘
  링과 같은 금속 문법).
- **★9-슬라이스로 늘려 쓴다.** 그러므로:
  - 네 모서리 장식은 **각 변에서 같은 두께**(대략 64px 안쪽)로 균일하게.
  - 중앙부는 **단순한 평면**이어야 한다 — 늘려도 뭉개지지 않도록 중앙에
    무늬·문양·그라데이션 변화를 넣지 마라.
  - 상하좌우 대칭.
- 판 안쪽은 반투명하지 않아도 된다(불투명 네이비).
- 배경(틀 바깥)은 투명(알파) 또는 크로마키.

### 이미지 5 — 버튼 판 (BACK 버튼용)
`ui_button_plate.png` / **512×256 (가로:세로 = 2:1)**

- "BACK" 글자가 얹히는 버튼 바탕이다. **글자를 그려 넣지 마라** — 게임이
  텍스트를 올린다.
- 모양: 이미지 4와 같은 문법의 작은 판 — 네이비 바탕 + 은색 테두리 + 금색
  리벳. 살짝 볼록해 보이면 좋다.
- **9-슬라이스로 늘려 쓴다.** 이미지 4와 같은 규칙: 테두리 두께 균일(대략
  48px 안쪽), 중앙은 단순 평면, 좌우 대칭.
- 배경은 투명(알파) 또는 크로마키.

---

끝. 이미지 5장 경로만 보고하고 종료하라.

---

## (참고 — 내 쪽 적용 계획, 코덱스와 무관)

1. 지도 v3 임포트 → `T_StageMap_Scroll_Flat` 교체
2. **지도가 눌린 채로 오면 코드 베일(mMapParchmentVeilColor 알파 0.46)을
   낮추거나 0으로 뺀다** — 이중으로 누르면 안 된다
3. 연결선 2장 임포트 → `WBP_FrontendMapLine` 클래스 디폴트(mSolidTexture/
   mDashedTexture) 교체. 타일링은 이미 가로 반복으로 되어 있다.
   현재 물고 있는 것은 SVN 공유 폴더 에셋(T_wm_path_connector 1129x196 /
   T_wm_path_dot 562x572)이라 덮어쓰지 않고 git 폴더에 새로 두고 참조만 돌린다.
   선 두께(mLineThickness)는 14 — 새 그림이 얇아 보이면 이 값도 같이 올린다
4. 프레임/버튼 임포트 → 9-슬라이스 마진 실측 후 범례 패널·BACK 버튼 스타일 적용
5. 빌드 + 지도 테스트 + 세로(720x1280) 실행 확인
