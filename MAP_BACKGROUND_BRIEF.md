# 코덱스에게 줄 지시문 — 지도 팝업 배경 이미지 (이미지만!)

> 코덱스에는 아래를 그대로 복붙한다. **코덱스는 PNG 1장만 만든다.**
> 임포트·WBP·코드는 코덱스가 하지 않는다 (이쪽에서 처리).
> 기존 책상 배경(world_map_desk_v2.png)은 폐기한다. 지도를 팝업으로 띄우는 방식으로 바꿨다.

---

## 복붙용 지시문

이미지 생성 작업이다. **코드·에셋·WBP·Config는 절대 건드리지 마라.**
ImageGen으로 아래 PNG 1장만 만들어 `시안4_7\지도_final\` 폴더에 저장하고 끝내라.
임포트도 하지 마라. 적용은 다른 쪽에서 한다.
**지도 그림(map_scroll_flat_v2.png)은 다시 그리지 마라. 이번엔 배경만이다.**

### 무엇을 만드는가
세로로 긴 지도 팝업이 화면 중앙에 뜨고, **그 뒤에 깔리는 배경 한 장**이다.
이전에 만든 나무 책상(램프·나침반·두루마리) 버전은 폐기한다. 소품이 폰 화면에서
너무 작아 안 보이고 화면만 잡아먹었다. 이번에는 **극단적으로 단순한 배경**이다.

### 이미지 — 지도 팝업 배경
- **정사각형 2048×2048.**
- 내용: 오브젝트를 그리지 마라. 램프·나침반·두루마리·책상 소품·건물·인물 전부 금지.
  아래 요소만으로 구성한다:
  1. 짙은 네이비-차콜 바탕 (현재 전투 HUD의 어두운 패널 색 계열)
  2. **중앙이 아주 은은하게 밝고 가장자리로 갈수록 어두워지는** 부드러운 비네트
  3. 아주 미세한 질감 — 거친 종이결 또는 결이 고운 나무결. **눈에 띄면 실패다.**
     멀리서 보면 그냥 어두운 단색으로 읽혀야 한다.
  4. (선택) 지도·항해 느낌의 아주 옅은 선 문양(나침반 장미·위도선 등)을
     바탕색과 거의 구분 안 될 만큼 낮은 대비로 깔아도 좋다. 티가 나면 빼라.
- **어둡고 채도를 낮게 유지하라.** 위에 얹히는 지도가 주인공이고 이 배경은
  지도를 돋보이게 하는 무대일 뿐이다. 배경이 눈에 들어오면 실패한 그림이다.

### ★비율 규칙 (가장 중요)
게임에서는 정사각형의 **세로를 화면 높이에 맞추고 좌우를 잘라낸다.**
폰 화면 비율이 기기마다 달라서 잘리는 폭이 달라진다:
- 긴 폰(21:9급): **가로 중앙 43%**만 보이고 좌우 28.5%씩 잘림
- 일반 폰(16:9): 가로 중앙 56%가 보임
- 태블릿(4:3): 가로 중앙 75%가 보임

따라서:
- 의미 있는 것은 전부 **가로 중앙 43% 안**에 두어라.
- 바깥 영역은 잘려도 아무 문제 없는 **균질한 연장**(같은 톤의 어두운 배경)으로만 채워라.
- 세로는 전부 보이므로 **위아래 끝까지 꽉 채워** 그려라. 여백·테두리 금지.
- 어느 위치에서 세로로 잘라도 그림이 성립해야 한다. 좌우 대칭이면 가장 안전하다.

### 출력
- 불투명 풀블리드 PNG. 알파·크로마키 필요 없다.
- 파일명: `map_popup_background_2048.png`

끝. 이미지 1장 경로만 보고하고 종료하라.

---

## (참고 — 내 쪽 적용 계획, 코덱스와 무관)

1. PNG 임포트 → `Map_Scrim` 배경 교체 (세로 맞춤 + 좌우 크롭 = cover-fit)
2. 책상 관련 로직 제거: 롤 막대 밴드 인셋(MapDeskRodTop/BottomStartFrac) 걷어내고
   지도 팝업을 화면 중앙 정렬로 되돌림
3. 원근 강도 완화: M_MapPerspective TopWidth 0.68 → 0.85 (책상이 없어져 강한 원근은 과함)
4. 지도 톤 다운(어둡게+채도↓) + 노드 아이콘 확대(Map_NodeMetrics) — 아이콘이 묻히는 문제
5. 빌드 + 지도 테스트 + 실행 확인
