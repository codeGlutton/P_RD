# 코덱스에게 줄 지시문 — 지도 이미지 생성 (이미지만!)

> 코덱스에는 아래를 그대로 복붙한다. **코덱스는 PNG만 만든다.**
> 임포트·WBP·코드·노드 배치는 코덱스가 하지 않는다 (이쪽에서 처리).

---

## 복붙용 지시문

이미지 생성 작업이다. **코드·에셋·WBP·Config는 절대 건드리지 마라.**
ImageGen으로 아래 PNG 3장만 만들어 `시안4_7\지도_final\` 폴더에 저장하고 끝내라.
임포트도 하지 마라. 적용은 다른 쪽에서 한다.

공통 톤: 기존 전투 HUD와 같은 판타지 보드게임 룩 — 따뜻한 목재, 네이비+은/금 금속 장식,
손그림 채색. 기존 시안(`시안4_7\지도\world_map_scroll_stage1_undead_castle_perspective_v06.png`)의
그림체를 유지한다.

### 이미지 1 — 빈 책상 배경 (고정 레이어용)
- 내용: 위에서 비스듬히 내려다본 나무 책상. 좌측에 램프·나침반·밀봉된 두루마리,
  우측에 가죽 스트랩. 상단과 하단에 **닫힌 양피지 롤 막대**가 가로로 하나씩.
- **중앙은 완전히 빈 책상**이다. 양피지도 지도도 그리지 마라 — 그 자리에 별도
  스크롤 레이어가 얹힌다.
- 해상도: 1920×1080. 기존 `world_map_tabletop_empty_desk_v04.png`가 이 역할인데,
  상·하단 롤 위치가 화면 상단 12%/하단 12% 안에 오도록 다시 그려라.

### 이미지 2 — 평평한 양피지 지도 본체 (스크롤 레이어용) ★핵심
- 내용: 세로로 긴 양피지 두루마리 지도 한 장. **완전히 평평한 직사각형으로
  그린다. 원근·사다리꼴·기울임 절대 금지** — 눕힌 원근은 게임 엔진이
  머티리얼로 실시간으로 건다. 이미지에 원근이 있으면 이중 왜곡이 된다.
- 양피지 테두리(틀 장식)는 이미지 네 변에 그대로 포함하고, 그림이 캔버스를
  가득 채운다(여백 없는 풀블리드).
- 지형 흐름(아래→위, 같은 숲이 서서히 오염되는 연속 변화, 바이옴 단절 금지):
  1. 하단 1/3: 밝은 숲속 마을 — 잔디·통나무집·상자·오솔길 (현재 전투맵 톤)
  2. 중단 1/3: 어두워지는 숲길 — 버려진 전초기지, 마른 나무, 안개 시작
  3. 상단 1/3: 묘비·해골 문양·청록 망령불 → **맨 위에 언데드가 점령한 성문**
     (1스테이지 보스 진입점, 뚜렷하게)
- 길: 아래 끝에서 위 성문까지 **오솔길 하나가 끊기지 않고** 이어진다. 길과 그
  주변 폭 40% 영역은 디테일을 옅게 — 그 위에 룸 노드 아이콘이 얹힌다.
- 비율: 세로:가로 = 3:1 (예: 1024×3072). 위·아래 끝 각 5%는 롤 막대 뒤로
  들어가는 여백이니 중요한 그림을 두지 마라.
- 풀블리드 직사각형이므로 크로마키/알파 여백은 필요 없다.
- 파일명: `map_scroll_flat_v2.png` 로 저장.

### 이미지 3 — 룸 아이콘 6종 아틀라스
- 전투(교차 검) / 엘리트(뿔 투구) / 보스(성문) / 상점(차양 좌판) / 보물(상자) /
  휴식(모닥불). 6개를 한 줄 또는 2×3 격자로.
- 스타일: 네이비 원판 + 금속 링 테두리(기존 노드 링과 동일 계열). 각 아이콘
  256×256, 여백 균일, 크로마키 배경.
- 기존 `room_icon_atlas_combat_v01.png`보다 실루엣을 굵고 단순하게 — 지도 위에서
  작게 보여도 종류가 구분돼야 한다.

끝. 이미지 3장 경로만 보고하고 종료하라.

---

## (참고 — 내 쪽 적용 계획, 코덱스와 무관)

1. PNG 임포트 → 텍스처 (평평한 지도는 T_StageMap_Scroll_Flat 교체)
2. 레이어 구조: 빈 책상(고정, Map_Scrim) / 평평한 지도+노드+연결선(RetainerBox 스크롤) / 범례·BACK(고정)
3. 눕힌 원근은 M_MapPerspective 리테이너 머티리얼이 화면 고정으로 건다 — 이미지·노드 배치는 평평
4. 탭 좌표는 C++ InverseMapPerspectiveUV 역변환으로 노드에 매핑
5. 빌드 + 지도 테스트 + 실행 확인
