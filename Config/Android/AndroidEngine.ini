[/Script/AndroidRuntimeSettings.AndroidRuntimeSettings]
; UE otherwise registers AdActivity even when the advertising SDK is absent.
bSupportAdMob=False

[OnlineSubsystemGooglePlay.Store]
; This free beta does not offer in-app purchases.
bSupportsInAppPurchasing=False

[SystemSettings]
; Beta compatibility trial for green-tinted video reported on Galaxy Z Flip7.
; Keep MP4/AndroidMedia but copy decoded frames through CPU memory instead of
; importing ImageReader/HardwareBuffer frames. Flip7 validation is still pending.
Android.EnableMediaPlayerBRIR=0
