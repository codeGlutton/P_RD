# -*- coding: utf-8 -*-
"""판 안 어디에 무엇을 놓을지 적어 둔 표. 시안에서 재어 적었다.

## 왜 손으로 적었나

채워진 시안에서 빈 판을 빼면 글자와 얼굴이 있던 자리가 나온다. 자리는
정확하다. 그런데 그중 어느 것이 이름이고 어느 것이 체력 막대인지는 생김새로
짐작해야 했고 -- 가로로 길면 막대, 네모나면 얼굴 -- 그 짐작이 판마다
어긋났다. 아군 줄마다 AP 보석 크기가 달라지고 적 초상 자리가 비었다.

역할을 정할 때도 같은 벽에 부딪혔고, 그때는 시안에 번호를 그려 눈으로 보고
표에 적어 해결했다. 여기서도 같다. 판 종류는 일곱 가지뿐이고 나머지는 그것의
되풀이라 한 장씩만 재면 된다.

## 어떻게 쟀나

눈대중이 아니다. 색으로 찾았다.

    초록 픽셀 -> 체력 막대     파란 마름모 -> AP 보석
    흰 글자   -> 이름과 수치   빈 판의 움푹한 곳 -> 초상 자리

    기사 줄에서 초록은 x=126 y=74 88x9, 보석은 x=126 부터 34.3 간격,
    "90/100"은 x=223 y=73 -- 전부 잰 값이다.

## 좌표계

판 왼쪽 위가 원점이고 단위는 시안 픽셀(1672x941 기준)이다. 배치할 때
CHROME_SCALE 을 곱해 설계 캔버스로 옮긴다.
"""

#: 시안 번호 -> 역할 -> 무엇을 어디에.
#:
#: rect 는 (x, y, w, h). pips 는 (x, y, 한 변, 간격, 개수).
#: 시안 번호 -> 역할 -> 무엇을 어디에.
#:
#: rect 는 (x, y, w, h). pips 는 (x, y, 한 변, 간격, 개수).
#:
#: 아래 값은 빈 판과 채운 판을 견줘 잰 것이다. 두 판이 한 픽셀도 다르지 않게
#: 만들어져 있어(채운 쪽이 빈 판 안만 채운 것이다), 빼면 채워 넣은 것만
#: 정확히 남는다. measure_slots.py 가 색으로 갈라 뽑았다.
SLOTS = {
    "01": {
        "round": {
            "text": (50, 50, 155, 40),
            "align": "center",
        },
        "objective": {
            "icon": (38, 44, 34, 58),
            "text": (84, 52, 220, 34),
            "align": "left",
        },
        "turn": {
            # 턴 칸도 마찬가지. 테 안쪽 원에 맞춘다.
            "portrait": (28, 21, 94, 95),
        },
        "party": {
            # 잰 값(26,8,90,104)은 테까지 포함한다. 우리 초상은 얼굴이
            # 네모를 꽉 채우는 그림이라 그대로 넣으면 금속 테를 덮는다.
            # 테 안쪽으로 들인다.
            "portrait": (35, 17, 72, 86),
            "name": (135, 18, 160, 30),
            "status_icon": (310, 20, 25, 25),
            "status": (341, 21, 95, 24),
            "hp_bar": (136, 50, 96, 18),
            "hp_text": (233, 50, 90, 20),
            "pips": (136, 74, 30, 34.3, 4),
            "ap_text": (380, 82, 58, 20),
        },
        "skill": {
            "cost": (98, 18, 44, 44),
            "icon": (37, 28, 80, 74),
            "name": (8, 146, 137, 28),
            "cooldown": (8, 182, 137, 24),
            "damage": (8, 210, 137, 24),
            "cost_line": (8, 252, 137, 28),
        },
        "enemy": {
            # 적 초상도 같다. 잰 값에 테가 섞여 있어 안쪽으로 들인다.
            "portrait": (36, 33, 85, 89),
            "name": (136, 30, 150, 28),
            "hp_bar": (138, 72, 82, 12),
            "hp_text": (227, 68, 76, 20),
            "defense_icon": (136, 102, 26, 26),
            "defense": (166, 104, 130, 22),
            "forecast_icon": (60, 142, 28, 28),
            "forecast": (94, 144, 180, 24),
            "status": (136, 176, 160, 20),
        },
        "endturn": {
            "text": (60, 32, 190, 44),
        },
    },
}


def slots(number, role):
    """이 시안 이 역할의 자리표. 없으면 None -- 부르는 쪽이 재어 쓴다."""
    return SLOTS.get(number, {}).get(role)
