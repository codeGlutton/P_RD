# -*- coding: utf-8 -*-
"""판의 자리와 크기를 갈아 끼운다. **손으로 적는 파일이다.**

## 왜 hud04_tuning.py 에 안 두나

그 파일은 apply_zones.py 가 통째로 다시 쓴다. 거기에 손으로 적어 두었더니
구역을 한 번 내려받아 넣는 순간 조용히 사라졌다 -- 턴 순서 판이 819 에서
713 으로 되돌아가 있었고, 화면을 다시 찍어 보고서야 알았다.

**기계가 만드는 파일에는 사람이 적은 값을 두지 않는다.**

## 값

판 이름 -> (w, h) 또는 (x, y, w, h).

갈아 끼운 그림이 시안 판과 비율이 다를 때 쓴다. 판을 그대로 두고 그림만
비율을 지켜 앉히면 굽는 쪽은 맞는데 구역 쪽은 판 전체에 늘여 보여 준다 --
눈으로 맞춘 자리와 나온 자리가 갈린다. 판을 그림에 맞추면 그럴 일이 없다.

넷을 적으면 자리까지 바꾼다. 판이 넓어질 때 크기만 바꾸면 오른쪽으로만
자라 옆 판을 밀고 들어간다.
"""

PLATE_SIZE = {
    # 메뉴 막대 1670 x 516
    "top_right_parchment": (392, 121),

    # 턴 순서 여섯 칸 + 양끝 넘김칸 819 x 158
    "top_center_turn_order": (426, 12, 819, 158),



    # 용병창은 세로다. 그림 630 x 1453 의 비(0.434)를 지켜 96 x 221.
    "bottom_status_left": (14, 712, 96, 221),
    "bottom_status_center": (118, 712, 96, 221),
    "bottom_status_right": (222, 712, 96, 221),

    # 가운데 AP 막대. 판에 없던 것이라 여기서 새로 세운다.
    "bottom_center_ap_bar": (486, 800, 700, 129),
}
